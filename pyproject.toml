[project]
name = "binance-os-strategies"
version = "0.1.0"
description = "Binance Agent OS multi-strategy trading bot (grid, regime rotation, volume spike, funding rate)"
requires-python = ">=3.10"
dependencies = [
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff>=0.4"]

[tool.pytest.ini_options]
pythonpath = ["."]
